package main

func main() {

	// First attempt worked fine for part a of the problem but took >22GB memory for part b
	FirstAttempt(80)
	//FirstAttempt(256)

	// Optimised second attempt
	SecondAttempt(80)
	SecondAttempt(256)
}
